{{- if .Values.modules.hdfs.persistence.dataNode.enabled -}}
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ include "hadoop.fullname" . }}-hdfs-dn
  labels:
    app: {{ include "hadoop.name" . }}
    chart: {{ include "hadoop.chart" . }}
    release: {{ .Release.Name }}
    component: hdfs-dn
spec:
  accessModes:
  - {{ .Values.modules.hdfs.persistence.dataNode.accessMode | quote }}
  resources:
    requests:
      storage: {{ .Values.persistence.dataNode.size | quote }}
{{- if .Values.modules.hdfs.persistence.dataNode.storageClass }}
{{- if (eq "-" .Values.modules.hdfs.persistence.dataNode.storageClass) }}
  storageClassName: ""
{{- else }}
  storageClassName: "{{ .Values.modules.hdfs.persistence.dataNode.storageClass }}"
{{- end }}
{{- end }}
{{- end -}}
